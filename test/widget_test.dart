// test/widget_test.dart — stub test, real tests require device
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (tester) async {
    expect(true, isTrue);
  });
}
